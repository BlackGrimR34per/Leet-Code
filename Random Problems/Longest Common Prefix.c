char*	longestCommonPrefix(char** strs, int strsSize)
{
	int	row;
	int	index;

	row = 0;
	index = 0;
	while (row < strsSize)
	
}

int	main(void)
{
	char	*strs[] = {"ABC", "CDEEF"};

	longestCommonPrefix(strs, 2);
}