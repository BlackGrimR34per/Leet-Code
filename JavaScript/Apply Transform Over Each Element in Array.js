/**
 * @param {number[]} arr
 * @param {Function} fn
 * @return {number[]}
 */
var map = function(arr, fn) {
    const output = [];
};

const arr = [1, 2, 3];
function plus(n){
    return n+1;
}
let mapping = map(arr, plus);