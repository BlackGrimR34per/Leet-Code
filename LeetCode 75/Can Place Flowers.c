#include <stdbool.h>
#include <stdio.h>

bool canPlaceFlowers(int* flowerbed, int flowerbedSize, int n) {
    
}

int main() {
    int input[] = {0, 0, 0, 0, 0};
    int size = 5;
    printf("%d", canPlaceFlowers(input, size, 1));
}